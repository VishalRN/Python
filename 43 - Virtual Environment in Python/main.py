# A virtual environment is a tool used to create an isolated Python environment on a single machine. It allows you to work on multiple projects with different dependencies and packages without conflicts between them.
import pandas as pd
print(pd.__version__)